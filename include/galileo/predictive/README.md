# galileo/predictive
The `galileo/predictive` folder contains all the CRTP derived classes for formulating model predictive optimal control problems. For example, this folder contains the derived classes of `SegmentModelBase` and `PhaseModelBase`.
