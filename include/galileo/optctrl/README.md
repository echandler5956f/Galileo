# galileo/optctrl
The `galileo/optctrl` folder contains all the CRTP derived classes for formulating specific optimal control problems. For example, this folder contains the derived classes of `SegmentModelBase` and `PhaseModelBase`.
