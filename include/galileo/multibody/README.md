# galileo/multibody
The `galileo/multibody` folder contains the CRTP derived classes that are specifically relevant to multibody systems. For instance, here, we define `StateMultiBody` and `NodeModelFreeFwdDynamics`, as well as multibody specific constraints such as friction cone and contact constraints.
